<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="16dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp"
    tools:context="jp.techacademy.hirokazu.tomoda.qa_app.QuestionDetailActivity">

    <Button
        android:layout_width="170dp"
        android:layout_height="40dp"
        android:text="お気に入りに登録"
        android:id="@+id/favoriteOnButton"
        android:enabled="true" />
    <Button
        android:layout_width="170dp"
        android:layout_height="40dp"
        android:text="お気に入りから削除"
        android:id="@+id/favoriteOffButton"
        android:layout_toRightOf="@id/favoriteOnButton"
        android:enabled="false" />

    <ListView
        android:layout_width="match_parent"
        android:layout_height="match_parent"

        android:id="@+id/listView"
        android:layout_below="@id/favoriteOnButton"
        android:layout_alignParentStart="true" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:src="@drawable/plus" />

</RelativeLayout>